{% extends 'master.twig' %}
{% block content %}
    <img src="/images/chase.png" />

    {% for post in posts %}
        <h2>{{ post.title }}</h2>
        <p>
            {{ post.content }}
        </p>
    {% endfor %}
{% endblock %}
